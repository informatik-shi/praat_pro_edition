# Отчёт базовой сборки — 2026-09-11

## Результат

Официальный Praat 7.0.02 собран без изменений C/C++ кода под Windows x64v1.
Получен `Praat.exe` в корне проекта. Бинарник не добавляется в Git.

| Параметр | Значение |
| --- | --- |
| Upstream commit | `6f3da9ef1d8cce0d5684afc104d02888dfc71b25` |
| Исходная метка | `custom-base-7.0.02` |
| Среда | MSYS2 CLANG64, `C:\msys64` |
| Clang | 22.1.8, target x86_64-w64-windows-gnu |
| GNU Make | 4.4.1 |
| pkg-config | 3.0.7 |
| Команда | `make PRAAT_ARCH=x64v1 -j4` |
| Версия EXE | `Praat 7.0.02 (August 26 2026)` |
| Размер | 96 382 976 байт |
| SHA256 | `88f81add9fbfb1e424d9f358e6d9634077d436d501b50b260127f73d2ccfdb4a` |

Повторная сборка: `bash scripts/build-windows.sh` из CLANG64.
Полный список установленных пакетов и лог находятся в `.local-build/`.
В сборке есть предупреждения Clang в upstream-коде, ошибок нет.

## Проверки

| Проверка | Результат |
| --- | --- |
| Синтаксис обоих shell-скриптов (`bash -n`) | Успешно |
| Компиляция и линковка | Код 0 |
| `Praat.exe --utf8 --version` | Версия 7.0.02 |
| `test/runAllTests_batch.praat` | 135 скриптов, код 0, итоговый OK |
| `dwtest/runAllTests_batch.praat` | 78 скриптов, код 0, итоговый OK |
| Окна Objects / Picture | Открываются и отображаются |
| Создание моно-тона 440 Гц | Успешно |
| View & Edit | Волна и спектрограмма отображаются |
| Выделение участка | Успешно |
| Команда воспроизведения | Выполнена без сообщения об ошибке |
| Запись микрофона / слышимость звука | Не проверено |

Количество скриптов посчитано по строкам `### executing` в логах верхнего
уровня; это не количество отдельных assert-проверок. Batch-наборы исключают
GUI-сценарии. Полный интерактивный набор тестов не запускался.

Повтор тестов: `bash scripts/test-windows.sh`. Скрипт использует только
штатные наборы, с `--FULL-TRUST` для временных файлов, `--no-pref-files`
и `--no-plugins`. Первый запуск без FULL-TRUST ожидаемо остановился на
файловых операциях; диагностические логи сохранены отдельно.

## Следующий этап

Определить первое функциональное изменение и реализовать его на этой
проверенной базе. Выбор направления кастомизации пока не получен.
