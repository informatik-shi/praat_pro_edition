# Журнал проекта

## 2026-09-12 — Отдельная ветка Windows 7 x86

1. От чистого `custom/main` (`8334209b6`) создана `codex/windows7-x86`.
2. Выбран MINGW32/GCC/MSVCRT; установлен `mingw-w64-i686-gcc`.
3. Добавлен профиль Makefile для i686, Windows API 6.1, PE subsystem 6.1,
   -O2 и статического runtime. x64-профиль сохранён.
4. Добавлены отдельные PowerShell/Bash скрипты: сборка чистого снимка Git
   в новой игнорируемой папке, batch-тесты, аудит DLL и упаковка исходников.
5. Создана инструкция по запуску, сборке и ограничениям памяти нетбука.
   Результаты фактической сборки и проверок будут записаны после выполнения.

## 2026-09-12 — Методичка по построению графиков на Lua

1. Сверены текущие `praat.plot`, API запросов Sound и руководство по слоям.
   Создана `docs/tutorials/lua-plots/README.md`: 10 последовательных занятий,
   ожидаемые результаты, задания, итоговая практика и памятка по ошибкам.
2. В examples сохранены 10 самостоятельных Lua-файлов, совпадающих с блоками
   методички, и measurements.tsv. Темы: линии, функции, scatter/bar, heatmap,
   чтение файла, экспорт/обновление, отсчёты Sound, панели, слои и context.
3. Проверены точные имена запросов Get time from sample number и Get value
   at sample number по исходникам Praat. Пример читает 441 отсчёт созданного Sound.
4. Batch-проверка на `dist/praat-custom.exe`: выполнены занятия 1–7 с заменой
   только show на no-op в проверочном окружении. Настоящие PNG-экспорты и
   Praat-команды выполнялись. У всех 10 примеров проверен синтаксис loadfile.
   Итог: 7 execution checks, 10 syntax checks, 8 PNG checks — PASS.
   Занятия 8–10 в этом этапе отдельно в GUI не исполнялись; API прикреплений
   проверялся автоматическими и ручными тестами на предыдущем этапе.
5. Проверены сигнатуры PNG и размер 1200×750 у примера экспорта. Два изображения
   просмотрены и включены в методичку; диапазон Y первого графика увеличен,
   чтобы легенда не закрывала вершину. Обновлённый пример выполнен повторно.
6. Добавлены ссылки из README/API, копирование методички в сборочный скрипт.
   Готовая копия находится в `dist/tutorials/lua-plots`. Исходный код и EXE
   не менялись; документация и примеры фиксируются в локальном Git.

## 2026-09-12 — Lua-визуализация, этап 2: редакторы Sound и Spectrogram

1. Проверены жизненный цикл FunctionEditor/FunctionArea, распределение областей,
   координаты спектрограммы и уведомления об изменении исходных данных.
2. Введён PlotHost: реестр открытых редакторов со стабильными id, текущими
   временными границами/выделением, независимыми местами panel и spectrogram.
   Состояние не содержит Lua-указателей и удаляется при закрытии редактора.
3. Добавлены `plt.editors()`, `fig:attach(id,target)`, `plt.detach(id,target)`.
   Данные копируются; только 1×1 axes. Слой спектрограммы допускает линии/точки;
   панель — все текущие типы графика. Неверные режимы и закрытые id отклоняются.
4. Рисование данных вынесено в общий Plot_drawData. SoundEditor/LongSound и
   SpectrogramEditor рисуют слой в своих временных/частотных координатах.
   Нижняя панель занимает 28% высоты с промежутком 2%; x совпадает с временем
   редактора, выделение отражается в панели. Во время redraw Lua не вызывается.
5. Добавлены меню Lua plots: show/hide и remove. Изменения данных, включая
   внутренние правки через штатное уведомление Praat, сбрасывают прикрепления.
   Ветка без анализа не устанавливает viewport скрытой области.
6. Первая пересборка выявила приватные методы координат FunctionArea;
   исправлено использованием штатного FunctionArea_setViewport/inqViewport.
   Из-за заголовков редакторов пересобраны зависимые библиотеки. Предупреждения
   полной сборки относятся к прежним TRACE/FFNet/MDS, итоговая линковка без ошибок.
7. Новый host-runner с настоящим LuaRuntime — PASS: копирование, два места,
   ошибки типа/сетки, detach, актуальный context, clear, устаревший id.
8. GUI: embedded-demo.lua из Lua Editor (F5) создаёт Sound. Траектории 440 Гц
   и 1000+500*t Гц совпадают с полосами спектрограммы; нижний синус виден.
   Выделение 0.506331..1.396154 с отражается в панели; Zoom to selection
   синхронно меняет обе области. Hide возвращает исходную компоновку.
9. GUI: spectrogram-demo.lua создаёт Spectrogram и открывает View (исправлена
   первоначальная ошибочная команда View & Edit). Красная линия 1200 Гц
   совпадает с полосой, ниже виден scatter. `SPECTROGRAM EMBEDDING: PASS`.
   Штатное предупреждение об отсутствующем phonetic font подтверждено;
   дополнительных шрифтов в рамках задачи не устанавливалось.
10. Регрессии на итоговом EXE: Lua API, plot PNG, host, траектории,
    Praat debugger, folding — PASS; Lua debugger также прошёл после интеграции.
    Полные upstream test/dwtest и отдельная GUI-проверка LongSound не повторялись.
11. Обновлены инструкция, упаковка двух demo и -Test. Готовый EXE:
    `dist/praat-custom.exe`, SHA-256
    `1816f5bd01bb688f1db0388fb819b14242d8dcbc73fc3a20d325563a937b5c3e`.
    Исходники, проверки и документация фиксируются в локальном Git.

## 2026-09-12 — Lua-визуализация, этап 1: отдельное окно

1. Пользователь выбрал оба способа встраивания, начиная с отдельного окна.
   Проверены Graphics, DemoEditor, PNG-экспорт и LuaRuntime. Зависимостей
   от Python, matplotlib, браузера и новых DLL не добавлено.
2. Добавлен встроенный модуль `require('praat.plot')`: Lua API figure/axes,
   подграфики, линии, точки, столбцы, тепловые карты, подписи, легенда, PNG.
3. Реализация разделена на модель, renderer, окно и Lua bridge в `sys/luaplot`.
   При show данные копируются; окна не удерживают указатели на Lua-state.
   Повторный show обновляет окно, close/reopen поддерживаются.
4. Добавлены приближение прямоугольника мышью, выбор axes, Zoom in/out и
   Reset view (Ctrl+H). Линии и ячейки ограничиваются границами axes.
5. Валидация: размеры сетки/изображения, плотные числовые массивы, NaN/Inf,
   равные длины x/y, прямоугольность матриц, цвета, возрастающие лимиты.
   Выявлен Melder_crash при 72 DPI внутри Graphics_init: до вызова движка
   введён allowlist 100/150/300/600 DPI и тест отклонения 72 DPI.
6. `scripts/test-lua-plot.sh` — PASS: PNG 800×600, сигнатура, четыре типа
   графиков, ошибки входа и восстановление после них. PNG просмотрен:
   графики, легенда, подписи и кириллица корректны.
7. `test-lua.sh`, `test-lua-debugger.sh` — PASS после подключения модуля.
   Итоговая пересборка с дополнительной проверкой сетки и повтор plot-tests
   — PASS, предупреждений компилятора нет. Полные upstream suites не повторялись.
8. GUI: demo.lua открыт из Lua Editor и запущен F5; четыре графика остаются
   после завершения Lua. Выделение участка приближает первый subplot,
   Ctrl+H возвращает исходный масштаб. `lifecycle.lua` подтвердил обновление
   того же окна, close дважды, reopen: `LUA FIGURE LIFECYCLE: PASS`.
9. Добавлены `docs/lua-plot.md`, demo и тесты в сборочный процесс. Обновлены
   README, Lua guide, CHANGES_CUSTOM, упаковка примера и документации в dist.
10. Сборка `dist/praat-custom.exe`, SHA-256:
    `77ee9e3bf87f966e42fb4d61f0f67318606d4dba1ebd217108eb6895c495d8d8`.
    Исходники фиксируются локально в Git; EXE остаётся в игнорируемом dist.
11. Следующий этап: использование этой модели/renderer внутри редакторов
    Sound/спектрограммы. В текущую реализацию это подключение ещё не входит.

## 2026-09-12 — Отладчик и точки останова Lua Editor

1. Изучены встроенный LuaRuntime, count-hook Stop, обработка Windows-событий,
   RichEdit и общая модель сворачивания. Новых внешних зависимостей не добавлено.
2. Добавлен `sys/luadebug`: реальные line hooks Lua перед строкой, состояния
   сеанса, Into/Over/Out с учётом глубины стека и текущей корутины.
3. Инспекция использует lua_getstack/getlocal/getupvalue и raw-перебор таблиц;
   не вызывает пользовательские метаметоды. Снимок ошибки снимается до lua_close.
4. В Lua Editor добавлены Debug menu, F9/мышь, якоря точек, F5/F6/F10/F11,
   Shift+F11/Shift+F5, панель Variables/Call Stack/Output Error и кнопки шагов.
   Остановка раскрывает скрытый блок; работающий исходник защищён от правок.
5. Новые интеграционные тесты связываются с настоящими библиотеками Praat/Lua:
   шаги, рекурсия, циклы, корутины, таблицы/upvalues, Stop/ошибка/повторный запуск,
   Praat API — PASS (`bash scripts/test-lua-debugger.sh`).
6. Сборка `.local-build/Praat-lua-debug-final.exe` — exit 0, без предупреждений.
   В GUI выявлено, что F10 приходит как WM_SYSKEYDOWN; обработка исправлена.
   После исправления runner пересобран и повторно прошёл.
7. Ручная проверка: breakpoint 6, Into → 2 с x=10, Over → 3 с y=11,
   Out → 7 с value=11; стек показывает функцию и вызывающую строку. Свёрнутая
   функция раскрывается. Кнопки Continue/Over и Shift+F11 работают.
8. На итоговом EXE проверены F10 до запуска и на паузе, F6 в бесконечном цикле
   (реальный n=68770000), Stop кнопкой и Shift+F5, повторный запуск, ошибка
   с подсветкой строки 2 и traceback. Точка мышью на строке 2 после вставки
   абзаца переехала на строку 3 и остановилась до инструкции с n=10.
9. `test-folding.sh`, `test-lua.sh`, `test-trajectories.sh`, `test-debugger.sh`
   — PASS. Первый запуск Lua regression ошибочно использовал старый корневой
   EXE; повтор выполнен с PRAAT_EXECUTABLE=./.local-build/Praat-lua-debug-final.exe.
   Полные upstream test/dwtest на этом этапе не повторялись.
10. Добавлены `docs/lua-debugger.md`, demo и упаковка в dist; runner включён
    в `build-windows.ps1 -Test`. Обновлены Lua guide, README и CHANGES_CUSTOM.
11. Итог: `dist/praat-custom.exe`, SHA-256
    `cd0517e3db30001788916bfb473d360aa19e8a57739fb6650db34df7adda8946`.
    Работающие пользовательские файлы не заменялись; Git фиксирует исходники,
    тесты и документацию, двоичный выпуск остаётся в игнорируемом dist.

## 2026-09-12 — Сворачивание блоков в двух редакторах

1. Проверены существующие RichEdit-виджеты, подсветка, gutter и якоря breakpoint.
2. Добавлен общий модуль сворачивания и независимая модель блоков Praat/Lua.
   Сворачивание реализовано как формат отображения, без удаления текста.
3. Добавлены отдельные кнопки −/+, меню View и F8/Ctrl+F8/Shift+F8.
4. В GUI обнаружено, что скрытые абзацы меняют нумерацию RichEdit. Номера строк,
   подсветка и breakpoint переведены на позиции полного исходника модели.
5. Проверено автоматическое раскрытие процедуры и цикла при остановке отладчика
   на исходной строке 5; сохранение свёрнутого файла оставляет все строки.
6. Проверены Lua: сворачивание/раскрытие мышью, вложенность, номера строк,
   выполнение скрытого кода, раскрытие перед редактированием и Undo.
7. Автотесты модели на 10 000 блоках и регрессионные тесты Lua, траекторий,
   ScriptDebugger прошли. Сборка выполнялась в отдельных exe без замены
   работающих пользовательских процессов. Инструкция: `docs/code-folding.md`.

## 2026-09-12 — Lua и отдельный редактор

1. Подтверждён объём: полноценный Lua с API команд Praat, отдельный от Praat Script Editor.
2. Проверены TextEditor, Interpreter, Stackel, регистрация команд и правила доверия.
3. Загружен официальный Lua 5.5.1; SHA-256 совпал с опубликованным:
   `1c4b4068d67061f2a2231ad2b5422e77acea1487ea9890f6320af614f4373dce`.
   Оригинальный архив распакован без изменения upstream-файлов, добавлен `praat.mk`.
   Зависимость записана отдельным Git-коммитом `25bc273a6`.
4. Библиотека встроена статически и компилируется как C++ для безопасного
   разматывания RAII при ошибках Lua. Реализованы вызовы настоящих команд через
   Stackel, результаты разных типов, выбор и перечисление объектов, UTF-8, pcall.
5. Добавлены Lua Editor и пункты New/Open/Run Lua. Реализованы подсветка,
   номера строк, F5/F7/Ctrl+T/Shift+F5, штатные сохранение и проверка несохранённых файлов.
6. Исправлено обновление доступных действий при переключении выделения через
   Lua: `praat_updateSelection` недостаточно без `praat_show` для готовых объектов.
7. Исправлена семантика корутин: служебное yield выполняется только в основном
   потоке Lua; дочерние корутины сохраняют обычный контракт resume/yield.
8. Тесты Lua прошли на Sound, Spectrum, Formant, Matrix; проверены числовые,
   векторные и матричные ответы, Unicode, require, корутины и ошибки команд.
   Повторно прошли тесты траекторий и интеграционные тесты существующего отладчика.
9. В нативном окне проверены подсветка, номера строк, F5 (Duration: 1.0),
   остановка бесконечного цикла по Shift+F5, последующее редактирование и traceback
   с именем файла, строкой 2 и вызывающей строкой 4.
10. API, ограничения и инструкции находятся в `docs/lua.md`; пример и лицензия
    Lua добавлены в упаковку. Итоговые регрессионные результаты фиксируются ниже.
11. Штатные `test/runAllTests_batch.praat` и `dwtest/runAllTests_batch.praat`
    завершились с exit 0. Добавлены тесты строк и массивов строк, синтаксической
    и runtime-ошибок, повторного запуска после ошибки — PASS.
12. В финальной GUI-проверке исправлена обработка меню при активном цикле:
    отключаются остальные меню и окна, события Lua Editor обслуживаются.
    Ручной тест Run → Stop на `while true do end` завершился сообщением остановки.
    Проверены F7 и штатный диалог несохранённых изменений.
13. Итоговый `dist/praat-custom.exe`: SHA-256
    `720f6bb9d4ed5a10e8c70548b0fe6f49a872652d8acea5c2f7897a663b049134`.
    Исходный код интеграции: коммит `4a37907e4`. Исходный работающий
    `Praat-custom.exe` в корне не перезаписывался; запускайте новую сборку из `dist`.

## 2026-09-11 — Инициализация

1. Проверена папка `C:\Users\PC\Documents\job\praatrus`: пуста, Git-репозитория
   не было. Инструкций AGENTS.md в проверенных родительских папках не найдено.
2. Проверен Git: `2.54.0.windows.1`; существующая глобальная идентичность
   настроена, глобальные настройки не менялись.
3. На официальной странице загрузки исходников проверена ссылка GitHub:
   прежний адрес `praat/praat` перенаправляет на `praat/praat.github.io`.
4. `git ls-remote` из песочницы завершился сетевой ошибкой. Клонирование с
   расширенным доступом успешно: `git clone --origin upstream
   https://github.com/praat/praat.github.io.git .`. Получено 6195 файлов.
5. Проверены HEAD, тег и полнота истории: commit
   `6f3da9ef1d8cce0d5684afc104d02888dfc71b25`, `v7.0.02`, shallow=false.
   После клонирования рабочее дерево чистое.
6. Прочитаны README.md, HOW_TO_BUILD_ONE.md, .gitignore и параметры Makefile.
   Официальный путь Windows — MSYS2, make и Clang/MinGW. В PATH не найдены
   make, gcc, clang, cmake; Cygwin/MSYS2 не найдены в проверенных стандартных
   путях `C:\cygwin64` и `C:\msys64`. Это не полный поиск по диску.
7. Созданы `custom/main` и метка `custom-base-7.0.02`; установлены локальные
   `core.autocrlf=false` и `push.default=nothing`. Первые попытки записи .git
   были отклонены песочницей; повтор с расширенным доступом успешен.
8. Добавлены CUSTOM_README.md, project-docs/JOURNAL.md,
   project-docs/BUILD_WINDOWS.md и project-docs/ROADMAP.md. Оригинальные
   исходники и документация авторов сохранены без изменений.
9. Для фиксации этапа выполняются `git diff --cached --check`, коммит
   `docs: initialize custom Praat project` и проверка чистоты рабочего дерева.
   Сам коммит служит записью успешной фиксации этапа.

Сборка и runtime-тесты не запускались: среда сборки пока не подготовлена.

## 2026-09-11 — Базовая сборка Windows и проверка

1. Пользователь подтвердил переход к сборке. Проверена чистота `custom/main`.
2. По https://www.msys2.org/ и https://www.msys2.org/docs/installer/
   выбран установщик `msys2-x86_64-20260611.exe`. API GitHub `releases/latest`
   указывал nightly; для установки использована датированная ссылка сайта.
3. Установщик и `.sha256` скачаны во временную папку. SHA256 проверен:
   `3150d7d9aa5dedd900a7f52300d4d918271e3a8fc47de94848818fd5a430e6b0`.
4. Выполнена установка в `C:\msys64` командой установщика
   `in --confirm-command --accept-messages --root C:/msys64`, код выхода 0.
5. Выполнен `pacman -Syu --noconfirm`: обновлён msys2-runtime, оболочка
   штатно завершена для обновления. В новой оболочке повторено обновление,
   затем запущена установка make, mingw-w64-clang-x86_64-clang и
   mingw-w64-clang-x86_64-pkgconf. Основные зеркала отдавали таймауты;
   pacman автоматически перешёл к следующим зеркалам.
6. Добавлены `scripts/build-windows.sh` и `scripts/test-windows.sh`.
   Сборка сохраняет версии инструментов, список пакетов, лог, версию EXE
   и SHA256. Тесты используют batch-варианты двух наборов авторов,
   отключают чтение/запись предпочтений и загрузку плагинов.
   `.local-build/` исключена из Git.

7. Установка пакетов завершилась кодом 0. Версии: Clang 22.1.8,
   GNU Make 4.4.1, pkg-config 3.0.7. Полный список —
   `.local-build/toolchain.txt` (локальный файл).
8. В CLANG64 выполнены `bash -n scripts/build-windows.sh
   scripts/test-windows.sh` и `bash scripts/build-windows.sh`.
   Сборка `make PRAAT_ARCH=x64v1 -j4` завершилась кодом 0.
   В upstream-коде есть предупреждения Clang; исправлять исходники не
   потребовалось. Лог: `.local-build/build-windows.log`.
9. Получен `Praat.exe`, 96 382 976 байт. `--version`:
   `Praat 7.0.02 (August 26 2026)`.
   SHA256: `88f81add9fbfb1e424d9f358e6d9634077d436d501b50b260127f73d2ccfdb4a`.
10. Первый запуск обоих batch-наборов остановился с кодом 127: Praat
    требовал `--FULL-TRUST` для записи/удаления тестовых файлов. Проверены
    операции с файлами в тестах, параметр добавлен в test-windows.sh.
    Первые логи сохранены как `.local-build/*-without-trust.log`.
    Запущен повтор обоих наборов.
11. Через computer-use запущен собранный EXE. Проверены окна Objects и
    Picture, создание моно-тона 440 Гц (0,4 с, 44100 Гц, амплитуда 0,2),
    открытие View & Edit, отображение волны и спектрограммы, выделение
    интервала примерно 0,099–0,249 с и команда воспроизведения без сообщения
    об ошибке. Слышимость звука и запись микрофона не проверялись.
    Первая попытка нажатия по accessibility-индексу дала ошибку координат;
    после нового снимка использованы координаты видимых элементов.

12. Повтор batch-тестов завершился успешно: `test` — 135 скриптов,
    `dwtest` — 78 скриптов по строкам `### executing` в логах. Оба процесса
    завершились кодом 0 и вывели итоговый ASCII-график OK. Общий код выхода
    test-windows.sh — 0. Логи: `.local-build/test.log`,
    `.local-build/dwtest.log`, `.local-build/test-results.txt`.
13. Обновлены инструкция сборки, описание и план проекта; добавлен
    `project-docs/BASELINE_WINDOWS.md`. Проверка `git diff --check` пройдена.
    Этап фиксируется коммитом `build: establish verified Windows baseline`.
    Исходный C/C++ код не изменён. EXE и промежуточные файлы исключены из Git.

Осталось для полного аппаратного smoke-теста: запись микрофона и проверка
слышимого результата. Для начала кастомизации готовы базовый EXE, команды
повторной сборки и успешно пройденные автоматические тесты.

## 2026-09-11 — встроенный Script IDE/debugger

1. Прочитано подробное техническое задание. Изучены ScriptEditor, TextEditor,
   GuiText, Interpreter, Formula, variablesMap, procedure/call stacks и Makefile.
   Анализ сохранён в `docs/architecture-analysis.md`, коммит `1952ab4ff`.
2. Добавлен необязательный observer настоящего Interpreter, state machine,
   breakpoint/step решения, Stop, реальные Variables/Call Stack, ограниченный
   read-only Watch. Ядро зафиксировано коммитом `8b1db8d92`.
3. Только ScriptEditor переведён на системный RichEdit. Добавлены лексер
   видимых строк, gutter, TOM anchors, сохранение breakpoint, Ln/Col и Debug.
   Интерпретатор не зависит от HWND; WinScriptEditor содержит Windows frontend.
4. Реализован кооперативный event pump в GUI-потоке. Во время исполнения
   исходник readonly, посторонние команды Praat отключены. Формы параметров
   и обычный Run используют исходный путь Praat.
5. Первые компиляции выявили отсутствие Melder_isDigit, несовпадение int/LONG
   в std::max и конфликт имени Polygon. Исправлены явной проверкой цифр,
   правильными типами и Win32 MoveToEx/LineTo. Повторная сборка успешна.
6. Созданы fixtures и C++ integration runner, связанный с библиотеками Praat.
   Проверены реальные остановки, вложенные процедуры, Over/Into/Out, цикл,
   пропущенные ветви, типы, Watch, continuation, Unicode, ошибка и Stop.
   Первый runner прошёл, но grep не прочитал UTF-16 stdout; добавлен --utf8.
7. Windows smoke выявил неверно выбранный родитель окна: внутренний form
   принимался за root и отключал собственный debugger. Разделены host/root.
   Для gutter добавлен SS_NOTIFY, проверены постановка и удаление кликом.
8. Проверены F5, F11, Shift+F11, Watch `.value * 2 = 8`, реальные frames.
   Исправлена перерисовка execution marker: RichEdit больше не переключает
   WM_SETREDRAW на каждом пустом тике; paused-loop явно обновляет children.
   После исправления видны жёлтая строка и стрелка; F10 переходит с 2 на 3.
9. Проверены Shift+F5 без закрытия приложения и runtime error строки 3:
   исходный диалог плюс файл/процедура/строка в Output/Error и навигация.
10. Watch дополнительно закрыт для неизвестных bare symbols (например stopwatch),
    добавлены regression assertions для этого случая и scientific notation.
    Интеграционные тесты повторно прошли.
11. Сгенерирован локальный fixture из 12000 присваиваний. Проверка выявила
    прокрутку дочернего gutter внутри RichEdit; добавлено закрепление его позиции.
    Устаревшие Variables/Error очищаются при открытии другого файла.
12. Созданы `BUILD_WINDOWS.md`, `build-windows.ps1`, архитектура debugger,
    CHANGES_CUSTOM и инструкция тестов. PowerShell entry действительно вызван
    с -Test: сборка и integration tests прошли, штатные suites запущены.
13. При попытке пересборки во время batch-тестов Windows отказал в записи
    открытого Praat-custom.exe. Тесты не прерывались; GUI-проверка собрана
    отдельно как `.local-build/Praat-ide-final.exe`. Это file lock, не ошибка C++.
    MSYS2 требует запуск вне ограниченного sandbox-профиля; запрошено штатное
    разрешение. Системные настройки безопасности не менялись.

14. Оба штатных набора на кастомной сборке завершились exit 0. Последние логи:
    test — 138 записей executing, dwtest — 78. GUI-поправки после этого
    затрагивали только отображение, численные suites повторно не запускались.
15. Большой файл: 12001 содержательная строка; переход к концу, F9, вставка
    строки переносит breakpoint с 12001 на 12002, Undo возвращает. Save
    сохранил исходный текст без gutter. Ctrl+G выбирает строку 6000; добавлена
    поправка, сохраняющая начало выделенной строки видимым.
16. Финальная сборка повторно получена одной командой. Проверены Windows PE
    imports (системные DLL/UCRT), запуск из dist и реальная остановка на строке 2.
    Временные preview-процессы закрыты. Итоговый EXE и SHA указаны в отчёте.
17. Изменения разбиты на коммиты: `8b1db8d92` ядро, `a6cd7c029` редактор,
    `36687411c` тесты, `abad0c6d0` сборка, `8f26040a5` навигация.

Подробные итоговые результаты и границы проверки: `docs/acceptance.md`.

## 2026-09-11–12 — траектории частот и формант

1. Уточнено требование: линии поверх спектрограммы (время/частота), отдельный
   CSV/TSV и ручное перемещение точек. Пользователь подтвердил оба решения.
2. Изучены FunctionEditor, RealTierArea, FormantGridArea, SoundArea,
   Spectrogram_paintInside, Formant и стандартная сериализация oo_*. Новая
   модель использует RealTier, GUI — штатные drag/Undo/Redo, без изменения
   алгоритмов анализа Sound и существующего Script IDE.
3. Добавлен FrequencyTrajectories: именованные линии, координаты времени/Гц,
   CSV/TSV, собственный текстовый/бинарный формат Praat. Проверяются числовые
   значения, границы, одинаковые времена и имена. Импорт допускает несортированные
   строки, Unicode, quoted CSV. Модель зафиксирована коммитом `1bf701e49`.
4. Добавлены команды Open/New/Save, запрос значения, Add point и преобразование
   Formant. Создан редактор с копией Sound, волной и спектрограммой; вычисление
   спектрограммы кэшируется по видимому интервалу и настройкам. Щелчок по точке
   выбирает её линию, перетаскивание меняет реальные данные объекта.
5. Первая сборка выявила неверное имя MelderFile_writeText: заменено на штатное
   MelderFile_writeText_e; добавлено подключение praatM.h. Повторная сборка
   успешна. Логи trajectories-build*.log. Исправлена инициализация SoundArea:
   копия становится доступна после functionChanged, а не сразу после create.
6. Созданы тесты и синтетический demo. CSV/TSV и native text/binary roundtrip,
   interpolation, duplicate/negative/out-of-domain rejection, кириллица и
   кавычки, создание/добавление точки, Formant conversion — PASS. В тестах
   исправлен синтаксис ожидаемой ошибки на штатный `asserterror`.
7. GUI: waveform и спектрограмма с F1/F2/F3; точка F1 перенесена с
   (0.5,650) в (0.5507889546351086,1100.3359158468). Undo возвращает исходную
   форму, Redo восстанавливает исправление. Через Objects → Save экспортирован
   `.local-build/trajectory-mouse-edited.csv`; на диске проверены новые координаты.
   Щелчок по F2 переключает активную линию. Команда воспроизведения сработала
   без ошибки; слышимость человеком не подтверждалась.
8. Исправлен порядок привязки данных при Undo/инициализации: area получает
   актуальные tiers до вызова родительского redraw. Regression debugger runner
   после добавления новых модулей — PASS.
9. Обновлены build-windows.ps1 (новые тесты в -Test), упаковка примеров CSV/demo,
   CHANGES_CUSTOM и руководство docs/frequency-trajectories.md. Выпуск собирается
   прежним MSYS2 CLANG64 Makefile без дополнительных DLL.
10. На итоговом EXE повторены новые tests (PASS) и штатные FormantGrid,
    Formant, Sound_to_Spectrogram (exit 0). Полные длительные suites на этом
    этапе не повторялись, поскольку алгоритмы анализа не менялись.
11. Проверка первого кадра обнаружила повторный вход в рисование через progress
    Sound_to_Spectrogram. По образцу SoundAnalysisArea добавлен
    autoMelderProgressOff и восстановление viewport. Коммит `e80b10e0f`.
    Пересборка и новый запуск из dist подтвердили корректный первый кадр.
12. Итоговый EXE: `dist/praat-custom.exe`, SHA-256
    `51b2246c173af524887df7422057e17292499d430f5c8d52eb9d466caaba8c77`.
    Редактор/команды: `8d6ecb418`, тесты/demo: `489b99a84`.
    Изменённый мышью объект сохранён в `.local-build/trajectory-mouse-edited.csv`;
    тестовый процесс с ним не закрывался при обновлении отдельного dist-процесса.

## Шаблон следующих записей

- Дата и задача.
- Выполненные действия и команды.
- Изменённые файлы и принятые решения.
- Проверки: команда, результат, ограничения.
- Следующий шаг.
